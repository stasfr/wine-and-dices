{
  "name": "@wine-and-dices/backend",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch server.js",
    "start": "node server.js",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "format": "oxfmt",
    "format:check": "oxfmt --check"
  },
  "devDependencies": {
    "oxfmt": "workspace:*",
    "oxlint": "workspace:*",
    "oxlint-tsgolint": "workspace:*"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
